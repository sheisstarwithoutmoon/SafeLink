specVersion: "v1"
serviceId: "saferide"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "saferide-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
